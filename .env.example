# Secrets — copy to .env and fill in real values
GH_TOKEN=
ANTHROPIC_AUTH_TOKEN=

# PostgreSQL (TCP via host.docker.internal)
DATABASE_URL=postgresql://postgres:<your-password>@host.docker.internal:5432/ibalgotrade
